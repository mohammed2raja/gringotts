{{#with (lookup sortInfo attr)}}
  <th data-sort="{{attr}}" class="sorting-control {{../class}} {{viewId}}">
    <a href="{{url routeName routeParams nextQuery}}" class="{{order}}">
      <span class="sorting-header">{{text}}</span><span class="indicator"></span>
    </a>
  </th>
{{/with}}
