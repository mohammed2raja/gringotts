{{#with (lookup sortInfo attr)}}
  <th data-sort="{{attr}}" class="sorting-control {{../class}} {{viewId}}">
    <a href="{{url routeName routeParams nextState}}" class="{{order}}">
      <span>{{text}}</span><span class="indicator"></span>
    </a>
  </th>
{{/with}}
